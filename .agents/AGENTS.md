# Project Rules

- **Organizer Dashboard Route**: The primary file for the organizer dashboard is `pages/organiser-dashboard.jsx`. The URL `/dashboard` is configured in `next.config.js` to redirect to `/organiser-dashboard` to avoid vagueness. Ensure all references respect this redirect or point directly to `/organiser-dashboard`.
